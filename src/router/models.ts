export const enum ROUTES {
  ROOT = '/',
  LOGIN = '/login',
  SIGNUP = '/signup',
  PRODUCTS = '/products',
  NEW = 'new',
  DETAILS = ':id',
  EDIT = ':id/edit',
  ERROR = '/error',
}

export const enum QUERY_KEYS {
  PRODUCTS = 'products',
  CATEGORIES = 'categories',
}
